# Salary Calculator

A simple system to calculate employee salaries based on attendance using Vue.js, Node.js, and MySQL.

## Setup

1. Create MySQL database using `database.sql`
2. Configure server/.env file
3. Install dependencies:
   ```bash
   cd server && npm install
   cd ../client && npm install
